# Privacy and local-data boundary

Video Knowledge 不是纯云端视频分析器，也不是“所有内容永不离开电脑”的封闭应用。它把确定性的媒体处理放在本地，但最终分析仍由用户选择的 Codex／ChatGPT 宿主环境完成。

## 本地执行

项目自带脚本会在用户电脑上执行：

- FFmpeg／FFprobe 媒体探测；
- 证据帧提取；
- Faster-Whisper 语音转写；
- SRT、TXT、JSON 和 Markdown 文件写入；
- 本地模型、下载目录、缓存和输出路径管理。

这些脚本不会主动把原始视频上传到本项目作者控制的服务器。本项目也没有自建服务器、遥测服务或用户数据库。

## 会发生的外部连接

安装或使用过程中可能访问：

- Python 包索引或已配置的软件源，用于安装锁定依赖；
- Hugging Face 的 `Systran/faster-whisper-small` 仓库，用于下载固定 revision 的模型；
- Windows Package Manager／FFmpeg 官方构建入口，用于补齐 FFmpeg；
- 用户提供的视频站点或现有素材获取器；
- 为核验公开视频主张而访问的公开网页；
- 运行 Skill 的 Codex／ChatGPT 服务。

具体网络请求还取决于用户的 Codex 配置、浏览器工具、下载器和系统软件源。

## 宿主 AI 边界

为了生成分析，Codex／ChatGPT 可能读取转写文字、证据帧、用户提示和项目上下文。这些内容如何传输、保留和使用，取决于用户所使用的 OpenAI 产品、账号类型、组织设置和当时的数据政策，不由本仓库单独控制。

不要仅因为“转写在本地完成”就假设后续分析也完全离线。

## 用户责任

- 只处理自己有权访问和分析的内容；
- 涉及客户、未成年人、商业机密、医疗或其他敏感信息时，先确认授权和宿主环境的数据设置；
- 发布分析前复核转写、人物身份、证据和引用；
- 删除本地素材、缓存或分析时，明确确认目标路径，不使用模糊的递归删除命令。

## 本仓库不会收集

公开仓库不应包含视频、客户文件、转写、分析结果、Cookie、浏览器会话、账号凭据、API 密钥、个人配置、模型权重或运行日志。若发现上述内容，请不要提交，并在本地清理后重新审计。
